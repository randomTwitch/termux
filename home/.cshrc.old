#(date&) will run any command as 1 process's sub not current or started from
#set works with or without = setenv only works without =
#sh -c "play-audio /system/media/audio/notifications/Signal.ogg"&;
#date +%s_%N_\#0
#lua -e "print(string.format('%x_%x_#0',`date +%s`,`date +%N`));--io.input():read();"
printf '\033[1;30m'
set prompt='\t'•••'\n':
unhash
#set q=`uptime`;
#set q="$q\n`date`";
#set q="$q\n`ps all`"
#echo $q | less;
#unset q;
#ps all;
#echo "/storage/emulated/0/Android/ is home for Android not /system /etc /proc /dev";
#tcsh -c "bash -c 'gh repo clone --help'"

#uptime >> ./terminalTime
#date +%s_%N_\#1
#lua -e "print(string.format('%x_%x_#1',`date +%s`,`date +%N`));--io.input():read();"
exit 0;
